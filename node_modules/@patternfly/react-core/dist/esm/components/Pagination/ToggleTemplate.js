import React, { Fragment } from 'react';
import PropTypes from 'prop-types';
const propTypes = {
  firstIndex: PropTypes.number,
  lastIndex: PropTypes.number,
  itemCount: PropTypes.number,
  itemsTitle: PropTypes.string
};
const defaultProps = {
  firstIndex: 0,
  lastIndex: 0,
  itemCount: 0,
  itemsTitle: 'items'
};

const ToggleTemplate = ({
  firstIndex,
  lastIndex,
  itemCount,
  itemsTitle
}) => React.createElement(Fragment, null, React.createElement("strong", null, firstIndex, " - ", lastIndex), ' ', "of ", React.createElement("strong", null, itemCount), " ", itemsTitle);

ToggleTemplate.propTypes = propTypes;
ToggleTemplate.defaultProps = defaultProps;
export default ToggleTemplate;
//# sourceMappingURL=ToggleTemplate.js.map